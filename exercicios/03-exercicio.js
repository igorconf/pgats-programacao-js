const nome = "Ada"
const idade = 1
const porte = "P"
const idadeMinima = 2

const adocao = idade >= idadeMinima || porte === "P"
                ? "sim"
                : porte === "P"
                ? "sim"
                : "não"

console.log(adocao)